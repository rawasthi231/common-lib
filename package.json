{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:jobs": "npm run dev --workspace=jobs",
    "start:backend": "npm run dev --workspace=backend"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.8"
  }
}
